let value: number = 25

// operator modulus -> untuk menentukan sisa hasil bagi
if (value % 2 === 0) {
    console.log(`${value} -> is even number`)
} else {
    console.log(`${value} -> is odd number`)
}